apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bridge-signer.fullname" . }}-test-connection"
  labels:
    {{- include "bridge-signer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bridge-signer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
