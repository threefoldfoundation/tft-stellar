apiVersion: v1
kind: Service
metadata:
  name: {{ include "bridge-signer.fullname" . }}
  labels:
    {{- include "bridge-signer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bridge-signer.selectorLabels" . | nindent 4 }}
