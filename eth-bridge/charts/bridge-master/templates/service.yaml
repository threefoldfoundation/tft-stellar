apiVersion: v1
kind: Service
metadata:
  name: {{ include "bridge-master.fullname" . }}
  labels:
    {{- include "bridge-master.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bridge-master.selectorLabels" . | nindent 4 }}
