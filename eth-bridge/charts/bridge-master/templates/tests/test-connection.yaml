apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bridge-master.fullname" . }}-test-connection"
  labels:
    {{- include "bridge-master.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bridge-master.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
