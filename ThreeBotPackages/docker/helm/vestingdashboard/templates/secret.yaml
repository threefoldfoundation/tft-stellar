{{- if .Values.identity.createsecret -}}
apiVersion: v1
kind: Secret
metadata:
    name: {{ include "vestingdashboard.fullname" . }}
type: Opaque
stringData:
    identity_tname: {{ .Values.identity.tname }}
    identity_email: {{ .Values.identity.email }}
    identity_words: {{ .Values.identity.words }} 


{{ end }}