apiVersion: v1
kind: Service
metadata:
  name: {{ include "vestingdashboard.fullname" . }}
  labels:
    {{- include "vestingdashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "vestingdashboard.selectorLabels" . | nindent 4 }}
